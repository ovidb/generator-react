// @flow
import styles from './<%= name %>.module.scss';

type Props = {

}

export const <%= nameUp %> = ({}: Props) = {
  return (
    <div className={styles.wrapper}>

    </div>
  );
}

<%= nameUp %>.displayName = '<%= nameUp %>';